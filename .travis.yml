language: android
jdk: oraclejdk7

android:
  components:
    - build-tools-21.1.2
    - android-16
    - extra-android-support
    - extra-google-google_play_services
    - extra-google-m2repository

script: ./gradlew build

# whitelist
branches:
  only:
    - gradle_build
